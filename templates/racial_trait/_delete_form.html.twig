<form method="post" action="{{ path('app_racial_trait_delete', {'id': racial_trait.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ racial_trait.id) }}">
    <button class="btn">Delete</button>
</form>
